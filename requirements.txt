# Core dependencies
requests>=2.28.0
pyaudio>=0.2.13
aiohttp>=3.8.0

# Windows-specific (for device fingerprinting)
pywin32>=305; sys_platform == 'win32'

# Secure credential storage
keyring>=23.0.0

# Google Cloud Vision OCR
google-cloud-vision>=3.0.0
google-auth>=2.0.0

# Future OCR providers (uncomment as needed)
# pytesseract>=0.3.10
# pillow>=9.0.0
# azure-ai-vision>=0.15.0
# boto3>=1.26.0  # AWS Textract
